defaultEntryPoints = ["http", "https"]

defaultEntryPoints = ["http"]

[entryPoints]
  [entryPoints.http]
  address = ":80"
#    [entryPoints.http.redirect]
#      entryPoint = "https"
  [entryPoints.https]
  address = ":443"
    [entryPoints.https.tls]

[acme]
email = "xsynkx@gmail.com"
storage = "acme.json"
entryPoint = "https"
onHostRule = true
onDemand = true

[acme.httpChallenge]
entryPoint = "http"

[docker]
endpoint = "unix:///var/run/docker.sock"
exposedbydefault = false